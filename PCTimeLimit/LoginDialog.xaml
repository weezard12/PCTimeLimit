<Window x:Class="PCTimeLimit.LoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PC Time Limit - Admin Login"
        Height="280"
        Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    <Border Background="White" Margin="16" Padding="16" CornerRadius="8" BorderBrush="#DDDDDD" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Enter Admin Credentials"
                       FontSize="16" FontWeight="Bold" Margin="0,0,0,12" HorizontalAlignment="Center"/>

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="_Username:"/>
                <TextBox x:Name="UsernameTextBox" Grid.Column="1" Height="28"/>
            </Grid>

            <Grid Grid.Row="2" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0" Text="_Password:"/>
                <PasswordBox x:Name="PasswordBox" Grid.Column="1" Height="28"/>
            </Grid>

            <TextBlock x:Name="StatusTextBlock" Grid.Row="3" Foreground="#CC0000" Margin="0,4,0,8" TextWrapping="Wrap"/>

            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="LoginButton" Content="Login" Width="90" Height="32" Margin="0,0,8,0"
                        Click="LoginButton_Click" IsDefault="True"/>
                <Button x:Name="CancelButton" Content="Cancel" Width="90" Height="32"
                        Click="CancelButton_Click" IsCancel="True"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
