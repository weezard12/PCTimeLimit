<Window
    x:Class="PCTimeLimit.LoginDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="PC Time Limit - Admin Login"
    Width="420"
    Height="280"
    Background="#F5F5F5"
    ResizeMode="NoResize"
    WindowStartupLocation="CenterScreen">
    <Border
        Margin="16"
        Padding="16"
        Background="White"
        BorderBrush="#DDDDDD"
        BorderThickness="1"
        CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                Grid.Row="0"
                Margin="0,0,0,12"
                HorizontalAlignment="Center"
                FontSize="16"
                FontWeight="Bold"
                Text="Enter Admin Code" />

            <Grid Grid.Row="1" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                    Grid.Column="0"
                    Margin="0,0,8,0"
                    VerticalAlignment="Center"
                    Text="Admin Code:" />
                <TextBox
                    x:Name="AdminCodeTextBox"
                    Grid.Column="1"
                    Height="28" />
            </Grid>

            <!-- Password row removed: admin code only login -->

            <TextBlock
                x:Name="StatusTextBlock"
                Grid.Row="3"
                Margin="0,4,0,8"
                Foreground="#CC0000"
                TextWrapping="Wrap" />

            <StackPanel
                Grid.Row="4"
                HorizontalAlignment="Right"
                Orientation="Horizontal">
                <Button
                    x:Name="LoginButton"
                    Width="90"
                    Height="32"
                    Margin="0,0,8,0"
                    Click="LoginButton_Click"
                    Content="Login"
                    IsDefault="True" />
                <Button
                    x:Name="CancelButton"
                    Width="90"
                    Height="32"
                    Click="CancelButton_Click"
                    Content="Cancel"
                    IsCancel="True" />
            </StackPanel>
        </Grid>
    </Border>
</Window>

