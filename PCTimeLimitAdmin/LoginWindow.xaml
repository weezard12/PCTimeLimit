<Window x:Class="PCTimeLimitAdmin.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="PCTimeLimit Admin - Login" 
        Height="450" 
        Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        
        <Style TargetType="Button">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                BorderBrush="{TemplateBinding BorderBrush}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004578"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,30">
            <TextBlock Text="PCTimeLimit Admin" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Foreground="#333333"/>
            <TextBlock Text="Parent Control Panel" 
                       FontSize="14" 
                       HorizontalAlignment="Center"
                       Foreground="#666666"
                       Margin="0,5,0,0"/>
            <TextBlock Text="Connecting to localhost:8888" 
                       FontSize="12" 
                       HorizontalAlignment="Center"
                       Foreground="#999999"
                       Margin="0,10,0,0"
                       x:Name="ServerInfoTextBlock"/>
        </StackPanel>
        
        <!-- Login Form -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Account Login" 
                       FontSize="16" 
                       FontWeight="SemiBold" 
                       Margin="0,0,0,20"
                       HorizontalAlignment="Center"
                       Foreground="#333333"/>
            
            <TextBlock Text="Username:" 
                       FontSize="12" 
                       Margin="0,0,0,5"
                       Foreground="#666666"/>
            <TextBox x:Name="UsernameTextBox" 
                     Margin="0,0,0,15"/>
            
            <TextBlock Text="Password:" 
                       FontSize="12" 
                       Margin="0,0,0,5"
                       Foreground="#666666"/>
            <PasswordBox x:Name="PasswordBox" 
                         Margin="0,0,0,20"/>
            
            <!-- Status Message -->
            <TextBlock x:Name="StatusTextBlock" 
                       FontSize="12" 
                       Margin="0,0,0,15"
                       TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Foreground="#E74C3C"/>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="CreateAccountButton" 
                    Content="Create Account" 
                    Width="120" 
                    Margin="0,0,10,0"
                    Click="CreateAccountButton_Click"/>
            <Button x:Name="LoginButton" 
                    Content="Login" 
                    Width="120" 
                    Margin="10,0,0,0"
                    Click="LoginButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
